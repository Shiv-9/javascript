// for loops 
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
} 
// index is not accessible outside the scope of the loop 

// while loops
//declaration
while (condition) {
    // code execution
  // condition updation  
}

// do while  :=> executes first then checks the condition 
do {
    // code execution 
    // condition updation
} while (condition);